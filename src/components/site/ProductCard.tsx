import {useState} from "react";
import {Plus,Check} from "lucide-react";
import type {Product} from "@/data/menu";
import {brl} from "@/data/business";
import {useCart} from "@/hooks/useCart";
const thumbnails:Record<string,[number,number]>={"copo-300": [14, 13], "pote-tradicional": [14, 195], "vitamina-300": [14, 394], "caixa-2l": [19, 390], "granola-1kg": [14, 490], "acerola-1kg": [28, 302], "pao-queijo-1kg": [19, 103]};
export function ProductCard({product,featured=false}:{product:Product;featured?:boolean}){const{add}=useCart();const[added,setAdded]=useState(false);const[variant,setVariant]=useState(product.variants?.[0]);const xy=thumbnails[product.id];return <article className="bg-card shadow-card"><div className="product-photo">{xy?<div role="img" aria-label={product.name} style={{width:80,height:80,borderRadius:24,backgroundImage:`url(/${product.id}-source.png)`,backgroundSize:"auto",backgroundPosition:`-${xy[0]}px -${xy[1]}px`,transform:"scale(2)"}} className="actual-thumb"/>:<img src={product.image} alt={product.name} loading="lazy"/>}</div><div className="p-6"><h3 className="text-xl">{product.name}</h3><p className="mt-2 min-h-10 text-sm text-muted-foreground">{product.description}</p>{product.variants&&<select aria-label={`Op̤̣o de ${product.name}`} value={variant?.id} onChange={e=>setVariant(product.variants?.find(v=>v.id===e.target.value))}>{product.variants.map(v=><option key={v.id} value={v.id}>{v.label}</option>)}</select>}<div className="mt-5 flex items-center justify-between gap-3"><strong className="text-2xl text-primary">{brl(variant?.price??product.price)}</strong><button onClick={()=>{add(product,variant);setAdded(true);window.setTimeout(()=>setAdded(false),1400)}} className="inline-flex items-center gap-2 rounded-full bg-primary px-4 py-3 text-sm font-bold text-white" aria-label={`Adicionar ${product.name}`}>{added?<Check size={18}/>:<Plus size={18}/>}<span aria-live="polite">{added?"Adicionado":"Adicionar"}</span></button></div></div></article>}
